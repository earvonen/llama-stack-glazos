[server]
transport = "http"
host = "0.0.0.0"
port = 8082
tools = ["host", "hostgroup", "problem", "trigger", "event", "item"]

[zabbix.production]
url = "${ZABBIX_URL}"
api_token = "${ZABBIX_API_TOKEN}"
read_only = true
verify_ssl = true
